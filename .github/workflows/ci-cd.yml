name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '17'

      - name: Build JAR
        run: mvn clean package -DskipTests

      - name: Build Docker Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/demo-app:${{ github.run_number }} .
          docker tag ${{ secrets.DOCKER_USERNAME }}/demo-app:${{ github.run_number }} ${{ secrets.DOCKER_USERNAME }}/demo-app:latest

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Push Image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/demo-app:${{ github.run_number }}
          docker push ${{ secrets.DOCKER_USERNAME }}/demo-app:latest

#      - name: Set up Kubeconfig
#        run: |
#          mkdir -p $HOME/.kube
#          echo "${{ secrets.KUBECONFIG }}" > $HOME/.kube/config
#
#      - name: Deploy to Kubernetes
#        run: |
#          kubectl apply -f k8s-deployment.yml
#          kubectl apply -f k8s-service.yml
#          kubectl rollout restart deployment demo-app




